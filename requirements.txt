# Core ML dependencies (required)
torch>=2.0.0
transformers>=4.30.0
scikit-learn>=1.3.0

# Data processing (required)
pandas>=2.0.0
numpy>=1.24.0

# Progress bars (required)
tqdm>=4.65.0

# Visualization (required for graphs and notebook)
matplotlib>=3.7.0
seaborn>=0.12.0

# Jupyter notebook (optional - only if running .ipynb)
jupyter>=1.0.0
ipykernel>=6.25.0

# For IPython display in notebook
ipython>=8.12.0

# API (optional - only if running api.py)
fastapi>=0.100.0
uvicorn>=0.23.0
